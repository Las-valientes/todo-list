const deleteFunction = {
  // removeTodo(e) {
  //   e.preventDefault();
  //   $(this).parent().remove();
  //   console.log($(this).parent())
  // 
  "hola" : {
    alert() {
      console.log("sii")
    }
  }
}

module.export = deleteFunction;
